-- Create sequences table

create table TB_SEQUENCES (
    SEQUENCE_NAME VARCHAR2(255 CHAR) NOT NULL,
    SEQUENCE_NEXT_VALUE NUMBER(19)
);

ALTER TABLE TB_SEQUENCES ADD CONSTRAINT PK_TB_SEQUENCES
    PRIMARY KEY (SEQUENCE_NAME)
;

-- Initialize sequences table

Insert into TB_SEQUENCES(SEQUENCE_NAME, SEQUENCE_NEXT_VALUE) VALUES ('RECOMMENDED_LINKS', 1);
Insert into TB_SEQUENCES(SEQUENCE_NAME, SEQUENCE_NEXT_VALUE) VALUES ('RECOMMENDED_KEYWORDS', 1);;

commit;